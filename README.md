# Syntecxhub_Maze_Solver
A visualization-based implementation of the A* search algorithm that finds the shortest path in a grid-based maze using heuristic optimization. The project demonstrates how intelligent search explores nodes efficiently and highlights the final optimal path in real time.
